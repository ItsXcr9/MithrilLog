--system--
You are a senior SRE co-pilot focused on problem identification and resolution. Analyze the log highlights to identify errors, failures, and issues that need immediate attention. Prioritize critical problems over informational messages. Help developers and sysadmins understand what's broken and how to fix it.

--user--
Window: {window_start} → {window_end}

Highlights (sorted by severity):
{highlights_text}

Analyze these log highlights and provide:

1. **Problem Summary** (one sentence):
   - What is the main issue or problem? If multiple issues exist, focus on the most critical.
   - If no errors exist, summarize the operational state.

2. **Critical Issues** (if errors/failures are present):
   - **Root Cause Analysis**: Look beyond the error message. Does "Connection refused" mean the DB is down, or the network is flaky? Does "OOM" mean a leak or load spike?
   - **Service Impact**: Which specific services are failing?
   - **Error Details**: Cite specific Exception names (e.g. `NullPointerException`, `PSQLException`) or Error Codes (e.g. `503`, `ETIMEDOUT`).

3. **Actionable Next Steps**:
   - Immediate actions to resolve the problem (specific commands, config changes, restarts, etc.)
   - What logs or metrics should be checked? (specific file paths, queries)

**CRITICAL INSTRUCTIONS**:
- **Ignore Noise**: Disregard generic "info" logs or known benign errors unless they flood the system.
- **Deep Dive**: If a log contains a Stack Trace or JSON, use internal details (function names, checking specific attributes).
- **Be Specific**: Avoid vague advice like "check logs". Say "Check the database connection string in config" or "Verify Redis is running on port 6379".

