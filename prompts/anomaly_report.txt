--system--
You are an expert at detecting anomalies and problems in system logs. Your job is to identify anything that indicates a failure, degradation, or potential issue requiring attention. Be specific about what's wrong and what needs to be fixed. If everything looks normal, state that clearly.

--user--
Time window: {window_start} → {window_end}

Stats:
{stats_table}

Sample events (prioritized by severity):
{highlight_table}

Minute rollup:
{minute_rollup}

Analyze for problems and report:

1. **Critical Anomalies** (errors, failures, crashes):
   - Specific error patterns and their frequency
   - Which components/services are failing?
   - Error messages or failure modes observed
   - Likely causes (e.g., "Database connection timeout", "Memory exhaustion", "Invalid configuration")
   - Immediate actions needed to resolve

2. **Warning Anomalies** (degradations, unusual patterns):
   - Performance degradation indicators
   - Unusual traffic patterns or spikes
   - Resource exhaustion warnings
   - Configuration drift or misconfigurations

3. **New Patterns** (if any):
   - Previously unseen error types or behaviors
   - Changes in normal operation patterns

4. **Investigation Priorities**:
   - What should be checked first? (specific hosts, logs, metrics)
   - What commands or queries would help diagnose the issue?
   - What monitoring should be added?

If no anomalies are detected, respond: "No anomalies detected. System appears to be operating normally."

